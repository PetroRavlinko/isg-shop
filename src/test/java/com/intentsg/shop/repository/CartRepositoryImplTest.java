package com.intentsg.shop.repository;

import com.intentsg.shop.model.Cart;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class CartRepositoryImplTest {

//    @Test
//    void test() {
//        // Given
//        CartRepositoryImpl cartRepository = new CartRepositoryImpl();
//        int someValue = 42;
//        Cart cart = new Cart();
//        long cartId = cartRepository.createCartAtDB(cart).getId();
//
//        // When
//        cartRepository.updateCartAtDB(cart, someValue);
//
//        // Then
//        assertEquals(someValue, cartRepository.getCartsFromDB().stream().filter(c -> c.getId() == cartId).findFirst().orElseThrow().getSmthToUpdate());
//    }

}